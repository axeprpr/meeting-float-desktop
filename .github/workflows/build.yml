name: Build

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: ubuntu-latest
    env:
      APP_VERSION: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nsis zip

      - name: Resolve version
        run: |
          if [ -z "${APP_VERSION}" ]; then
            APP_VERSION="$(sed -n 's/.*\"version\": \"\\([^\"]*\\)\".*/\\1/p' package.json | head -n1)"
          else
            APP_VERSION="${APP_VERSION#v}"
          fi
          echo "APP_VERSION=$APP_VERSION" >> "$GITHUB_ENV"

      - name: Build Windows distribution
        run: ./windows/build-win-dist.sh

      - name: Build NSIS installer
        run: |
          cd windows
          makensis -DAPP_VERSION=$APP_VERSION installer.nsi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: meeting-float-sciter-win-x64-v${{ env.APP_VERSION }}
          path: |
            dist/meeting-float-sciter-v${{ env.APP_VERSION }}-win-x64.zip
            dist/Meeting-Float-${{ env.APP_VERSION }}-Setup.exe
          if-no-files-found: error

      - name: Create GitHub release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: Meeting Float v${{ env.APP_VERSION }}
          files: |
            dist/meeting-float-sciter-v${{ env.APP_VERSION }}-win-x64.zip
            dist/Meeting-Float-${{ env.APP_VERSION }}-Setup.exe
